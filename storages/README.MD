# Storages Directory
Place your custom DataStorage implementation files in this directory.
Ensure they use the `@storages.register("name")` decorator.

## External Dependencies (Optional)
If your custom storage plugin requires third-party Python libraries, 
place a `requirements.txt`file in this directory listing those dependencies: