# Providers Directory
Place your custom DataProvider implementation files in this directory.
Ensure they use the `@providers.register("name")` decorator.

## External Dependencies (Optional)
If your custom provider plugin requires third-party Python libraries, 
place a `requirements.txt`file in this directory listing those dependencies: